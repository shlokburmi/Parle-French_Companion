export default function Placeholders({ title }) {
  return (
    <div className="screen-container" style={{ textAlign: 'center', paddingTop: '100px' }}>
      <h2 className="section-title">{title}</h2>
      <p className="section-sub">This feature is coming soon.</p>
    </div>
  );
}
